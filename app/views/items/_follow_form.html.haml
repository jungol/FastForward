.follow-form#follow_form{id: "item-" + item.id.to_s}
  / - if !(user_signed_in?)
  /   .list--badge.v-small-text#follow-count-list_{id:list.id}
  /     = list.users.count
    
  /   %button.btn.btn-primary.btn-large.new_subscription{"data-target" => "#myModal", "data-toggle" => "modal", :type => "button"}
  /     %i.fa.fa-bookmark
  /     Sign in to get updates when there are new findings
  / # = render partial: 'shared/sign_in_modal'

  - if current_user.following?(item)
    = render partial: 'items/unfollow', locals: {item: item}
  - else
    = render partial: 'items/follow', locals: {item: item}