= form_for(@collection) do |f|
  .field
    = f.label :name
    = f.text_field :name

  .field
    = f.label :parent_id
    = f.collection_select :parent_id, Collection.order(:name), :id, :name, include_blank: true
  = f.submit