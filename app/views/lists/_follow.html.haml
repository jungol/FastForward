= form_for(current_user.subscriptions.build(list_id: list.id), remote: true) do |f|
  = f.hidden_field :list_id
  =button_tag(class: "btn btn-invisible") do
    %i.fa.fa-bookmark-o
  %ul 
    %li GET UPDATES
    %li= pluralize(list.users.count, 'subscriber')
